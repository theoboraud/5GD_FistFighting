namespace Enums
{
    public enum GameState
    {
        InPlay,
        MainMenu,
        CharacterSelectMenu,
        SettingsMenu,
    }

    public enum PlayerState
    {
        Alive,
        Dead,
        Ready,
        NotReady
    }

    public enum GameMode
    {
        Race,
        Versus
    }
}
